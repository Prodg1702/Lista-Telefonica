import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Lista-Telefonica',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
